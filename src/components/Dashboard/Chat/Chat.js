import React, { Component } from 'react';
import MessageList from '../../MessageList';

class Chat extends Component {
  render() {
    return (
      <div>
        <MessageList />
      </div>
    );
  }
}

export default Chat;